import { section } from "./source.js";

/**
 * Whole arrangements: a section of a page rather than a piece of one.
 *
 * A pattern carries no controls. It is an arrangement, and the words in it
 * belong to the screen rather than to the pattern, so a knob here would invite
 * editing copy in the wrong place.
 */
export default {
  title: "Patterns",
  parameters: { layout: "fullscreen" },
};

export const Patterns = {
  name: "All patterns",
  render: section("patterns"),
  parameters: {
    docs: {
      description: {
        story:
          "The bento and the dark tile. A bento is a composition of unequal tiles, not a grid of equal ones: if the section could have been generated by mapping over an array, it is a card grid and the design language is gone (layout.md 4).",
      },
    },
  },
};
